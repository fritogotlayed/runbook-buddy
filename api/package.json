{
  "name": "runbook-buddy-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "nodemon --exec 'npm run build && node dist/index.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^3.27.4",
    "fastify-cors": "^6.0.3"
  },
  "devDependencies": {
    "@sinclair/typebox": "^0.23.4",
    "@types/node": "^17.0.22",
    "nodemon": "^2.0.15",
    "typescript": "^4.6.2"
  }
}
