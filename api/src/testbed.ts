async function main() {
  // NOTE: Intentionally left empty in commits. This is meant as a place for developers
  // to quickly test things out while in watch mode without the need for a full request / response cycle.
}

if (import.meta.main) {
  await main();
}
